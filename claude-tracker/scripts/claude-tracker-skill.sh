#!/bin/bash
# Claude Tracker — UserPromptSubmit hook script
# Skill tracking is handled by the Stop hook via transcript parsing,
# which captures the full qualified name (e.g. pilot:prompt-architect).
# This script is kept as a no-op placeholder for future per-prompt tracking.

exit 0
