{
  "hooks": {
    "UserPromptSubmit": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "input=$(cat) && prompt=$(echo \"$input\" | jq -r '.prompt // \"\"') && skill=$(echo \"$prompt\" | sed -n 's|^/shopify-theme-toolkit:\\([^ ]*\\).*|\\1|p') && [ -n \"$skill\" ] && [ -n \"${TOOLKIT_ANALYTICS_URL:-}\" ] && curl -s -X POST \"${TOOLKIT_ANALYTICS_URL}\" -H 'Content-Type: application/json' -d $(jq -n --arg u \"${USER:-unknown}\" --arg s \"$skill\" --arg key \"${TOOLKIT_ANALYTICS_KEY:-}\" --arg p 'shopify-theme-toolkit' '{user:$u,skill:$s,plugin:$p,api_key:$key}') > /dev/null 2>&1 & true"
          }
        ]
      }
    ],
    "PostToolUse": [
      {
        "matcher": "Write|Edit",
        "hooks": [
          {
            "type": "command",
            "command": "input=$(cat) && file=$(echo \"$input\" | jq -r '.tool_input.file_path // empty') && [[ \"$file\" == *.liquid ]] && shopify theme check \"$file\" --fail-level error 2>&1 | tail -30 || true"
          }
        ]
      },
      {
        "matcher": "Write|Edit",
        "hooks": [
          {
            "type": "command",
            "command": "input=$(cat) && file=$(echo \"$input\" | jq -r '.tool_input.file_path // empty') && [[ \"$file\" == *.css ]] && npx stylelint \"$file\" 2>&1 | tail -20 || true"
          }
        ]
      },
      {
        "matcher": "Write|Edit",
        "hooks": [
          {
            "type": "command",
            "command": "input=$(cat) && file=$(echo \"$input\" | jq -r '.tool_input.file_path // empty') && [[ \"$file\" == *.js ]] && npx eslint \"$file\" 2>&1 | tail -20 || true"
          }
        ]
      }
    ]
  }
}
