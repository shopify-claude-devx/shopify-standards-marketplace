# Design Context: {feature-name}
_Auto-generated by parse-figma.js — do not edit manually_

---

## CSS Typography Comment Block

Paste this verbatim at the top of `assets/{feature-name}-stylesheet.css` **before writing any rules**:

```css
/*
  Typography from Figma:
  {role}          : {family}, {px}px ({rem}rem), weight {weight}, lh {lineHeight}, {color}
  background      : {hex}
*/
```

---

## Typography

| Role | Family | px | rem | Weight | Line Height | Letter Spacing | Color | Node ID |
|------|--------|----|-----|--------|-------------|----------------|-------|---------|
| {role} | {family} | {px}px | {rem} | {weight} | {lh} | {ls} | {hex} | {nodeId} |

---

## Layout

### .{section-name}  —  Node `{nodeId}`  —  "{Layer Name}"
- **flex-direction**: `row` | `column`
- **gap**: `{n}px`
- **padding**: `{top}px {right}px {bottom}px {left}px`
- **background**: `{hex}`
- **width**: `{n}px`
- **height**: `{n}px`

### .{section-name}__{element}  —  Node `{nodeId}`  —  "{Layer Name}"
- **flex-direction**: `row`
- **gap**: `{n}px`
- **align-items**: `CENTER`

---

## Layer Structure

Every node listed here needs a `data-figma-id` attribute in Liquid.

```
Node {nodeId} → .{section-name} [section]
  Node {nodeId} → .{section-name}__{element} [FRAME]
    Node {nodeId} → .{section-name}__{child} [TEXT] "{text content}"
    Node {nodeId} → .{section-name}__{child} [IMAGE] "{asset-name}"
    Node {nodeId} → {% render 'icon-{name}' %} [SVG]
```

---

## Assets

### Images
| Asset Name | Node ID | Source | Viewport |
|------------|---------|--------|----------|
| `{section-name}-{element}` | `{nodeId}` | ✅ source resolved | desktop |

### SVGs (inline snippets)
| Snippet Name | Node ID | Output Path |
|--------------|---------|-------------|
| `icon-{name}` | `{nodeId}` | `snippets/icon-{name}.liquid` |

---

## Responsive Differences (mobile overrides)

| Property | Desktop | Mobile | Selector | Node ID |
|----------|---------|--------|----------|---------|
| `flex-direction` | `row` | `column` | `.{section-name}__{element}` | `{nodeId}` |
| `font-size` | `32px` | `24px` | `.{section-name}__{element}` | `{nodeId}` |
| `padding` | `80px 60px 80px 60px` | `40px 20px 40px 20px` | `.{section-name}` | `{nodeId}` |
